{
  "name": "backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch --experimental-strip-types src/server.ts",
    "start": "node --experimental-strip-types src/server.ts",
    "build": "tsc -p tsconfig.json --noEmit",
    "migrate:supabase": "node --experimental-strip-types src/migrateLocalToSupabase.ts"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.104.1",
    "zod": "^4.3.6"
  }
}
